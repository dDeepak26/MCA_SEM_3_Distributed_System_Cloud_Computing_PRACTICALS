# Database Connection Properties

# JDBC URL for MySQL
db.url=jdbc:mysql://localhost:3306/rmi_demo

# Database username
db.username=root

# Database password
db.password=root

# JDBC driver class name
db.driver=com.mysql.cj.jdbc.Driver

# Connection pool settings (optional)
db.max_pool_size=10
db.timeout=30000